import { StyleSheet } from "react-native-web"

const styles = StyleSheet.create({
    shadow: {
        shadowColor: "#000",
        shadowOffset: {
            width: 0,
            height: 8,
        },
        shadowOpacity: 0.50,
        shadowRadius: 4.65,
  
        elevation: 6,
    }
  })
  export default styles